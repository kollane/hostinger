# Production Environment Variables Template
# IMPORTANT: Copy to .env.prod and update with real values
# NEVER commit .env.prod to Git!

# ============================================================================
# Database Credentials
# ============================================================================
# IMPORTANT: Use strong, randomly generated passwords in production
DB_PASSWORD=CHANGE_ME_SuperSecureProductionPassword123!

# ============================================================================
# JWT Secret
# ============================================================================
# IMPORTANT: Generate a strong random secret (min 64 characters)
# Example: openssl rand -hex 64
JWT_SECRET=CHANGE_ME_a8f5f167f44f4964e6c998dee827110c3e51c9e5f3a7f0d8e2b4c9a1f5e8d7b3

# ============================================================================
# Grafana
# ============================================================================
# Admin password for Grafana dashboards
GRAFANA_PASSWORD=CHANGE_ME_GrafanaAdminPassword456!

# ============================================================================
# Application Settings
# ============================================================================
NODE_ENV=production
SPRING_PROFILES_ACTIVE=prod

# ============================================================================
# Monitoring
# ============================================================================
# Prometheus retention period (days)
PROMETHEUS_RETENTION_DAYS=30

# ============================================================================
# SSL/TLS
# ============================================================================
# For production, use Let's Encrypt certificates
# Self-signed certificates are for testing only
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 1. Generate strong random passwords:
#    openssl rand -base64 32
#
# 2. Use secrets management in production:
#    - HashiCorp Vault
#    - AWS Secrets Manager
#    - Azure Key Vault
#
# 3. Rotate secrets regularly (every 90 days)
#
# 4. Never commit .env.prod to Git:
#    Add to .gitignore: .env.prod
#
# 5. Restrict file permissions:
#    chmod 600 .env.prod
