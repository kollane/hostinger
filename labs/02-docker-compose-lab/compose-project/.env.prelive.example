# ==========================================================================
# PRELIVE Environment Variables
# ==========================================================================
# Kopeeri see fail: cp .env.prelive.example .env.prelive
# Käivitamine: docker-compose -f docker-compose.yml -f docker-compose.prelive.yml --env-file .env.prelive up -d
# ==========================================================================

# PostgreSQL Credentials (PRELIVE - tugevamad paroolid)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=prelive_strong_password_change_me_123!
POSTGRES_USER_DB=user_service_db
POSTGRES_TODO_DB=todo_service_db

# JWT Secret (PRELIVE - tugev secret)
JWT_SECRET=prelive-jwt-secret-change-to-random-base64-string

# Application Ports (PRELIVE - standard)
USER_SERVICE_PORT=3000
TODO_SERVICE_PORT=8081
FRONTEND_PORT=8080

# Logging (PRELIVE - production-like)
LOG_LEVEL=info
SPRING_LOG_LEVEL=INFO

# Resource Limits (PRELIVE)
USER_SERVICE_CPU=0.5
USER_SERVICE_MEM=512M
TODO_SERVICE_CPU=1.0
TODO_SERVICE_MEM=1G
