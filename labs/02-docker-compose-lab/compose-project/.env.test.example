# ==========================================================================
# TEST Environment Variables
# ==========================================================================
# Kopeeri see fail: cp .env.test.example .env.test
# Käivitamine: docker-compose -f docker-compose.yml -f docker-compose.test.yml --env-file .env.test up -d
# ==========================================================================

# PostgreSQL Credentials (TEST - lihtsamad paroolid)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=test123
POSTGRES_USER_DB=user_service_db
POSTGRES_TODO_DB=todo_service_db

# JWT Secret (TEST - võib olla lihtsam)
JWT_SECRET=test-secret-key-not-for-production

# Application Ports (TEST - standard)
USER_SERVICE_PORT=3000
TODO_SERVICE_PORT=8081
FRONTEND_PORT=8080

# Database Ports (TEST - avatud localhost'ile)
POSTGRES_USER_PORT=5432
POSTGRES_TODO_PORT=5433

# Logging (TEST - verbose)
LOG_LEVEL=debug
SPRING_LOG_LEVEL=DEBUG
