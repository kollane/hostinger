# ==========================================================================
# PRODUCTION Environment Variables
# ==========================================================================
# Kopeeri see fail: cp .env.prod.example .env.prod
# OLULINE: Muuda KÕIK paroolid ja secretid tugevateks!
# Käivitamine: docker-compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env.prod up -d
# ==========================================================================

# PostgreSQL Credentials (PRODUCTION - MUUDA KINDLASTI!)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD_MIN_32_CHARS
POSTGRES_USER_DB=user_service_db
POSTGRES_TODO_DB=todo_service_db

# JWT Secret (PRODUCTION - MUUDA KINDLASTI!)
# Genereeri: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_RANDOM_BASE64_STRING_32_BYTES

# Application Ports (PRODUCTION)
USER_SERVICE_PORT=3000
TODO_SERVICE_PORT=8081
FRONTEND_PORT=80

# Logging (PRODUCTION - minimal)
LOG_LEVEL=warn
SPRING_LOG_LEVEL=WARN

# Resource Limits (PRODUCTION - strict)
USER_SERVICE_CPU=1.0
USER_SERVICE_MEM=512M
TODO_SERVICE_CPU=2.0
TODO_SERVICE_MEM=1G
POSTGRES_USER_CPU=1.0
POSTGRES_USER_MEM=512M
POSTGRES_TODO_CPU=1.0
POSTGRES_TODO_MEM=512M

# ==========================================================================
# TURVALISUS
# ==========================================================================
# ❌ ÄRA COMMIT'I .env.prod FAILI GIT'I!
# ✅ Hoia seda turvaliselt (password manager, Vault, etc.)
# ✅ Genereeri tugevad paroolid:
#    - POSTGRES_PASSWORD: min 32 tähemärki, juhuslik
#    - JWT_SECRET: base64 encoded random string (32 bytes)
#
# Tugevate paroolide genereerimine:
#   openssl rand -base64 32  # JWT secret
#   openssl rand -base64 48  # PostgreSQL password
# ==========================================================================
